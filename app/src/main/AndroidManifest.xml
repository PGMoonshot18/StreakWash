<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pg.moonshot.streakwash">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="Streak Wash"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <!--<intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>-->
        </activity>
        <activity android:name=".LoginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LeaderboardActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".GoalActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".RewardsActivity" />
        <activity android:name=".GoalActivity"/>
        <activity android:name=".StreakActivity"></activity>
        <receiver
            android:name=".AlarmWashReceiver"
            android:enabled="true"
            android:exported="true" />
    </application>

</manifest>